export interface IItem {
    url: string;
    description?: string;
    domain: string;
    price: number;
    oldPrice: number;
    chatID: number;
    update?: Date;
}

//export default class Item implements IItem{

//}